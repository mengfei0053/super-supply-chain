# Backend Scripts Guide

Scope: `backend/scripts/`.

This directory contains helper assets for data or file conversion workflows.

- Keep scripts deterministic and document required inputs in comments or nearby docs.
- Do not commit large generated outputs here.
- Preserve sample files such as `example.et` unless the supported workflow changes.
