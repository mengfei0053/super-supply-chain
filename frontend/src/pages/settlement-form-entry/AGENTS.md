# Settlement Form Entry Guide

Scope: `frontend/src/pages/settlement-form-entry/`.

This resource maps to backend settlement form entry endpoints.

- Keep create/list resource names aligned with backend routes.
- Preserve field names expected by the backend models/controllers.
- When changing list filters or pagination, check `dataProvider.ts` query serialization.
