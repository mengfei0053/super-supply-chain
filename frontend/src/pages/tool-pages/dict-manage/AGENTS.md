# Dict Manage Page Guide

Scope: `frontend/src/pages/tool-pages/dict-manage/`.

This resource manages dictionary key/value/type data.

- Keep fields aligned with `models.BaseDict`.
- Preserve map lookup expectations for `/dict-manage/map/:type`.
- Avoid renaming values shown elsewhere without checking dependent selects.
