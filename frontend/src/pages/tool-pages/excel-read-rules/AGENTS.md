# Excel Read Rules Page Guide

Scope: `frontend/src/pages/tool-pages/excel-read-rules/`.

This resource configures how uploaded Excel files are parsed.

- Preserve rule object shape used by backend `ExcelReadRuleInfos`.
- Be careful with nested rule inputs; backend parsing expects stable JSON structure.
- Check menu/table options when changing rule creation or editing flows.
