# Frontend Components Guide

Scope: `frontend/src/components/`.

Shared UI components live here.

- Keep components reusable and independent from page-specific route assumptions.
- Prefer MUI and React Admin primitives already used by the project.
- Keep prop names explicit and typed.
- Avoid embedding API calls in shared components unless the component is specifically an API-backed input.
