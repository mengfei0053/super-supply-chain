# Frontend Public Assets Guide

Scope: `frontend/public/`.

Static assets here are copied directly by Vite.

- Keep filenames stable when referenced by `index.html`, manifest data, or deployed clients.
- Prefer optimized assets.
- Do not place runtime uploads or generated build output here.
